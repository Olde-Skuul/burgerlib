//
// Simple color shader DirectX9 version
// Attach to a vertex array of positions
//

float4 Color : register(c0);			// Attached color

float4 main() : COLOR0 {
	// Simply get a color and draw it
	return Color;
}
